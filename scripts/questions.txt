# 한 줄에 질문 하나. 빈 줄과 # 주석은 무시됩니다.
# 아래는 예시 — 실제 테스트할 질문으로 교체하세요.
요청 본문을 넘기는 파라미터 content / data / files / json 의 차이는?
httpx에서 HTTP/2를 켜는 방법과 기본값은?
Client(base_url=...)을 설정하면 요청 URL은 어떻게 합쳐져?
httpx.Headers는 어떤 자료구조야?
httpx.MockTransport는 무엇이고 어떻게 써?
httpx에서 재시도(retries)를 설정하는 방법은?
verify 파라미터에 넣을 수 있는 값과 기본 동작은?
httpx.Timeout 객체의 네 가지 값은 각각 무엇을 제한해?
ASGITransport / WSGITransport는 무엇이고 어떻게 써?

왜 requests와 달리 raw 바이트 전송에 data 대신 content라는 별도 파라미터를 뒀지?
왜 HTTP/2를 기본 활성화하지 않았지?
왜 표준 urljoin과 다르게 요청 경로 앞의 '/'를 무시하고 항상 base path 아래로 붙이지?
왜 헤더를 dict가 아니라 리스트 기반으로 저장하지?
왜 mocking을 monkeypatch가 아니라 transport 교체 방식으로 지원하지?
왜 재시도를 연결 실패로만 한정하고 Client 옵션으로 두지 않았지?
왜 시스템 CA 저장소 대신 certifi 번들을 기본으로 쓰지?
왜 단일 total timeout 대신 단계별 timeout을 두었지?
왜 테스트 서버를 띄우지 않고 앱을 직접 호출하는 방식을 제공하지?

files 파라미터가 multipart 본문으로 인코딩되는 절차를 알려줘
http2=True일 때 HTTP/2 연결이 협상되어 요청이 처리되는 절차를 알려줘
상대 URL이 최종 URL로 만들어지고 params가 합쳐지는 절차를 알려줘
Client 기본 헤더와 요청별 헤더가 합쳐져 전송되는 절차를 알려줘
MockTransport로 요청이 처리되는 절차를 알려줘
retries가 설정된 상태에서 연결 실패가 재시도되는 절차를 알려줘
verify 설정이 SSLContext로 만들어져 TLS 연결에 적용되는 절차를 알려줘
Client 기본 timeout과 요청별 timeout이 결정되어 transport에 전달되는 절차를 알려줘
ASGITransport로 요청이 앱까지 전달되어 응답이 만들어지는 절차를 알려줘

본문 인코딩 로직과 multipart 구현은 어디에 있어?
HTTP/2 관련 설정과 h2 의존성 체크는 어디에 있어?
URL 파싱·조합과 QueryParams 구현은 어디에 있어?
Headers 클래스와 기본 헤더 값은 어디에 정의돼 있어?
MockTransport와 transport 인터페이스는 어디에 정의돼 있어?
retries 인자는 어디서 받아 어디로 전달돼?
SSL 컨텍스트 생성 코드는 어디에 있어?
Timeout 클래스와 기본 timeout 상수는 어디에 있어?
ASGI/WSGI transport 구현은 어디에 있어?